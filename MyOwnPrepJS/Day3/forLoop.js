// For loop in javascript is used to do some repetative task

let arr = [2,5,6,8,10]
let sqaredArr =[];
for(var i = 0;i<arr.length;i++){
    sqaredArr.push(arr[i]*arr[i]);
    console.log(sqaredArr[i]);
}