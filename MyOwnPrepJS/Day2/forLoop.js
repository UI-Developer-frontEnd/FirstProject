// Loops are the statements that we can use to contol a flow of the program and to do some repeatative task
// For Loop: for()
// Square the each number in an array

let newArr = [
    newobj ={
        car:"Hyundai",
        price:2000000,
        year:'2023'
    },
    'names',20000
]
for(i=0;i<newArr.length;i++){
    let arr =newobj;
    if(newobj.car == 'Hyundai'){
        console.log(arr.year);
    }
}